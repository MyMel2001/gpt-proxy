PORT=5533
HEADLESS=true
USER_DATA_DIR=./browser-profiles
# Ollama settings (used ONLY for robust response extraction from page HTML) (fake IP used here, use your own!)
OLLAMA_HOST=http://100.118.11.83:11434
# (any local model that can handle HTML → text extraction)
OLLAMA_MODEL=qwen3.5:9b
